<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bvito</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">

    <link rel="stylesheet" href="index.css">
</head>

<body>

    <div class="container">
        <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
            <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-dark text-decoration-none">
                <span class="fs-4">Bvito</span>
            </a>

            <div>
                <a href="/my" class="btn btn-outline-dark">Мои объявления</a>
            </div>
        </header>
    </div>

    <main>
        <div class="container products-container">
            <div class="row g-3 products-row">

                {{!-- Форма поиска --}}
                <div class="col-4">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">
                                Поиск
                            </h5>

                            <form action="/search" method="GET">
                                {{!-- Название --}}
                                <div class="mb-3">
                                    <input name="title" value="{{title}}" type="text" class="form-control"
                                        placeholder="Что ищете?">
                                </div>

                                {{!-- Категория --}}
                                <select name="category" class="form-select">
                                    <option value="">Без категории</option>
                                    {{{options}}}
                                </select>

                                {{!-- Сортировка по цене --}}
                                <div class="my-3 d-flex justify-content-between align-items-center">
                                    <div>Цена: </div>

                                    <div>
                                        <input name="sort" value="1" type="radio" class="btn-check" id="sort-up"
                                            autocomplete="off" {{#if sortUp}} checked {{/if}}>
                                        <label class="btn btn-outline-secondary" for="sort-up">
                                            <img src="icons/arrow-up.svg">
                                        </label>

                                        <input name="sort" value="-1" type="radio" class="btn-check" id="sort-down"
                                            autocomplete="off" {{#if sortDown}} checked {{/if}}>
                                        <label class="btn btn-outline-secondary" for="sort-down">
                                            <img src="icons/arrow-down.svg">
                                        </label>
                                    </div>

                                </div>

                                {{!-- Кнопка Найти --}}
                                <div class="d-flex justify-content-end">
                                    <button type="submit" class="btn btn-primary">Найти</button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>

                {{!-- Список товаров --}}
                <div class="col-8 products-list">
                    {{#each array}}
                    <div class="card mb-3">
                        <div class="row g-0">
                            <div class="col-md-4">
                                <img src="{{this.image}}">
                            </div>
                            <div class="col-md-8">
                                <div class="card-body">

                                    <h5 class="card-title">{{ this.title }}</h5>

                                    {{#if this.asNew}}
                                    <span class="badge text-bg-primary">Как новенький</span>
                                    {{/if}}

                                    <div class="text-muted">
                                        {{this.category}}: {{this.price}} руб
                                    </div>
                                    <h6 class="card-subtitle my-2 text-muted">{{this.description}}</h6>
                                </div>
                            </div>
                        </div>
                    </div>
                    {{/each}}
                </div>
            </div>
        </div>
    </main>
</body>

</html>